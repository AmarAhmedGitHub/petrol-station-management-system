# TODO: تحسين منطق الربط بين واجهات الاستشعار وواجهات الإدارة

## الهدف
تحسين الربط الذكي بين واجهات مراقبة الاستشعار وواجهات إدارة الأجهزة (الخزانات، المضخات، إلخ) لتحقيق نظام أتمتة متقدم.

## التحليل الحالي

### واجهة مراقبة الاستشعار (sensor_monitoring.py)
- عرض مستويات الخزانات مع التحكم اليدوي
- عرض حالة المضخات مع التحكم اليدوي
- تحديث تلقائي كل 5 ثوانٍ
- تسجيل قراءات المستشعرات

### واجهة الإدارة (main_management.py)
- إدارة المحطات، الموظفين، التعييئات، الفواتير، التوريد، الصيانة
- عرض بيانات فقط (قيد التطوير للعديد من الأقسام)

### قاعدة البيانات المحسنة (database_enhanced.py)
- جداول شاملة للخزانات، المضخات، المستشعرات، الصيانة
- وظائف للتسجيل والقراءة والتحديث

## التحسينات المطلوبة

### 1. ربط ذكي بين المستشعرات والأجهزة
- [ ] إنشاء نظام ربط تلقائي بين قراءات المستشعرات وحالة الأجهزة
- [ ] تحديث حالة الخزانات تلقائياً بناءً على قراءات المستشعرات
- [ ] ربط المضخات بالخزانات المرتبطة بها
- [ ] تنبيهات تلقائية عند انخفاض مستويات الوقود

### 2. تحسين واجهة مراقبة الاستشعار
- [ ] عرض معلومات مفصلة عن كل خزان (نوع الوقود، السعة، الموقع)
- [ ] عرض معلومات مفصلة عن كل مضخة (الموظف المسؤول، الخزان المرتبط)
- [ ] رسوم بيانية لتتبع مستويات الوقود مع الوقت
- [ ] تنبيهات بصرية للمستويات المنخفضة أو المرتفعة

### 3. تحسين واجهة الإدارة
- [ ] إضافة قسم إدارة الخزانات مع الربط بالمستشعرات
- [ ] إضافة قسم إدارة المضخات مع الربط بالموظفين والخزانات
- [ ] إضافة قسم إدارة المستشعرات (PTS2، ATG)
- [ ] عرض حالة الصيانة والتنبيهات

### 4. نظام التنبيهات والإشعارات
- [ ] تنبيهات عند انخفاض مستوى الوقود (< 20%)
- [ ] تنبيهات عند توقف المضخات
- [ ] تنبيهات صيانة دورية
- [ ] إشعارات في الوقت الفعلي

### 5. تحسين الأتمتة
- [ ] تحسين خوارزمية التسوية التلقائية
- [ ] ربط أفضل بين قراءات المستشعرات والمبيعات
- [ ] تحديث تلقائي لحالة الأجهزة

## خطة التنفيذ

### المرحلة 1: تحسين واجهة مراقبة الاستشعار
1. إضافة معلومات مفصلة عن الأجهزة
2. تحسين الرسوم البيانية والتنبيهات
3. ربط أفضل مع قاعدة البيانات

### المرحلة 2: تطوير إدارة الأجهزة
1. إضافة قسم إدارة الخزانات
2. إضافة قسم إدارة المضخات
3. إضافة قسم إدارة المستشعرات

### المرحلة 3: نظام التنبيهات
1. تنفيذ نظام التنبيهات الأساسي
2. إضافة إشعارات في الوقت الفعلي
3. تكامل مع واجهات المستخدم

### المرحلة 4: تحسين الأتمتة
1. تحسين خوارزميات التسوية
2. ربط أفضل مع المستشعرات الحقيقية
3. تحسين الأداء والموثوقية

## الملفات المطلوب تعديلها
- `pages/sensor_monitoring.py`
- `pages/management/main_management.py`
- `core/database_enhanced.py`
- `core/automation.py`
- `core/sensor_api.py`
